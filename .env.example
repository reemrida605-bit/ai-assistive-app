# Get your key from https://aistudio.google.com/app/apikey
# This key is exposed to the browser — restrict it in Google Cloud Console.
VITE_GEMINI_API_KEY=

# Optional fallback used when Gemini returns 429 / RESOURCE_EXHAUSTED.
# Get your key from https://console.groq.com/keys
VITE_GROQ_API_KEY=
